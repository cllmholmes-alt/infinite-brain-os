# Outputs

Every produced artifact lands here with lineage frontmatter linking back to the workflow,
agent, or session that produced it. Outputs are products, not knowledge: durable signal gets
promoted out of an output into the right namespace through the normal promotion path.

Disposable test artifacts also default here, never to knowledge surfaces.

This folder ships empty except for the starter example.
