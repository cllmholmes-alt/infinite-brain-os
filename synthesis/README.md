# Synthesis (root)

Cross-namespace derived thinking lives here: documents that combine more than one namespace's
canon into a new interpretation. Single-namespace synthesis belongs in that namespace's own
`synthesis/` folder instead.

Root synthesis is never canon. When a synthesis stabilizes and earns promotion, it moves into
a namespace canon through the normal operator-approved path.

This folder ships empty.
