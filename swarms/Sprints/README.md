# Swarm Sprints

Each approved swarm sprint gets one folder here: charter, plan, launch sheet, acceptance
gates, approval receipt, and wave receipts. A sprint is an execution package hanging off a
parent task with `mode: swarm`; it is never a second backlog. See `_system/swarm-sprint-rules.md`.

This folder ships empty.
