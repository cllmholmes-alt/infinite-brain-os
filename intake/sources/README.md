# Intake Sources

Captured source records land here, organized by channel subfolder (for example `web/`,
`ideas/`, `youtube/`). A source record is the raw capture plus its capture metadata. Intake
owns the trail, not the truth: the destination namespace owns what it accepts.

This folder ships empty. See `intake/README.md` for the capture-to-routing flow.
