# Intake Processed Receipts

Every routed intake item leaves a processed receipt here: what came in, the routing decision,
and the destination that accepted it. Receipts are append-only and validated for completeness
(routing decision link plus destination block).

This folder ships empty.
