# Parties: Clients

One pointer node per client, built from `parties/_template.md`. Client nodes carry the
relationship record (who, since when, what scope); contracts and revenue numbers stay in the
systems that own them.

This folder ships empty.
